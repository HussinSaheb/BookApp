<% @books.each do |book| %>
<div class="row books">
  <div class="col-md-6">
      <p><%= book.title %></p>
      <p><%= book.genre %></p>
  </div>
  <div class="col-md-6">
  <%= link_to 'edit', edit_book_path(book) %>
  <%= link_to 'view', book_path(book) %>
  </div>

</div>

<% end %>
